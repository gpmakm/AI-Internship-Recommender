<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard</title>
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif; background-color: #f0f2f5; margin: 0; padding: 20px; }
        .container { max-width: 960px; margin: auto; padding: 20px; }
        .header { text-align: center; margin-bottom: 30px; }
        .header h1 { color: #2c3e50; }
        .user-info-card { background-color: #fff; padding: 20px; border-radius: 10px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); margin-bottom: 30px; text-align: center; }
        .user-info-card p { margin: 5px 0; color: #555; }
        .recommendations-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 20px; }
        .card { background-color: #fff; border-radius: 10px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); padding: 20px; display: flex; flex-direction: column; justify-content: space-between; transition: transform 0.2s; }
        .card:hover { transform: translateY(-5px); }
        .card h3 { color: #007bff; margin-top: 0; }
        .card p { margin: 5px 0; font-size: 0.9em; }
        .card-footer { display: flex; justify-content: space-between; align-items: center; margin-top: 15px; }
        .total-score { font-size: 1.2em; font-weight: bold; color: #28a745; }
        .card-footer .score-details { font-size: 0.8em; color: #777; }
        .logout-btn { background-color: #dc3545; color: white; padding: 10px 15px; border-radius: 5px; text-decoration: none; font-weight: bold; transition: background-color 0.2s; }
        .logout-btn:hover { background-color: #c82333; }
    </style>
</head>
<body>

    <div class="container">
        <div class="header">
            <h1>Welcome, <%= user.username %>!</h1>
            <a href="/logout" class="logout-btn">Log Out</a>
        </div>

        <div class="user-info-card">
            <p><strong>Email:</strong> <%= user.email %></p>
            <p><strong>Preferred Location:</strong> <%= user.jobPrefLocation %></p>
            <p><strong>Skills:</strong> <%= [user.skill1, user.skill2, user.skill3].filter(s => s).join(', ') %></p>
        </div>

        <h2>Your Top Internship Recommendations</h2>

        <% if (recommendations.length > 0) { %>
            <div class="recommendations-grid">
                <% recommendations.forEach(rec => { %>
                    <div class="card">
                        <div>
                            <h3><%= rec.title %></h3>
                            <p><strong>Location:</strong> <%= rec.location %></p>
                            <p><strong>Sector:</strong> <%= rec.sector %></p>
                            <p><strong>Distance:</strong> <%= rec.distance %></p>
                        </div>
                        <div class="card-footer">
                            <span class="total-score">Score: <%= rec.totalScore.toFixed(2) %></span>
                            <span class="score-details" title="Skill: <%= rec.scores.skillScore %>, Distance: <%= rec.scores.distanceScore %>, Qualification: <%= rec.scores.qualificationScore %>, Experience: <%= rec.scores.experienceScore %>">
                                (View Details)
                            </span>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <p style="text-align: center; color: #777;">No suitable internships found for you at this time.</p>
        <% } %>
    </div>

</body>
</html>